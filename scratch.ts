import { createClient } from '@supabase/supabase-js'; import { env } from './server/lib/env'; const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function test() { const { data, error } = await supabase.auth.admin.generateLink({ type: 'magiclink', email: 'patient@example.com' }); console.log('data:', JSON.stringify(data, null, 2)); console.log('error:', error); } test();
